<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.coindcx</groupId>
  <artifactId>trade-volume-aggregator</artifactId>
  <name>Trade Volume Aggregator</name>
  <version>1.0.0-SNAPSHOT</version>
  <description>Aggregate traded volume per user using events from Aeron and Kafka</description>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>17</release>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.3</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>com.coindcx.aggregator.App</mainClass>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <maven.compiler.target>17</maven.compiler.target>
    <slf4j.version>2.0.13</slf4j.version>
    <maven.compiler.source>17</maven.compiler.source>
    <chronicle-map.version>3.25ea6</chronicle-map.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <aeron.version>1.44.1</aeron.version>
    <jackson.version>2.17.0</jackson.version>
    <logback.version>1.5.6</logback.version>
    <kafka.version>3.7.0</kafka.version>
  </properties>
</project>
